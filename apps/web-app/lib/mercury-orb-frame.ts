/**
 * Single source of truth for the mercury orb chrome box.
 * Prompt (Ear) and post-triad dock must use identical dimensions so the ball does not jump.
 */
/** Video ref: soft teal-tinted chrome around orb (still reads on #111827 page). */
export const MERCURY_ORB_FRAME_CLASS =
  'relative isolate h-[220px] w-full overflow-hidden rounded-2xl border border-[#5EEAD4]/25 bg-[#111827]/50 p-3 shadow-[0_0_0_1px_rgba(17,24,39,0.8)] md:h-[280px] md:p-4';
