/**
 * **Panelist DNA** — re-export from **`@alchemist/shared-engine/triad-panelist-prompt.ts`** (Vitest there).
 *
 * **Canon (do not swap):** **DEEPSEEK → ATHENA** (harmonic / matrix), **LLAMA → HERMES** (rhythm / LFO),
 * **QWEN → HESTIA** (texture / saturation-filter). Matches **`PANELIST_ALCHEMIST_CODENAME`** /
 * **`triad-panel-governance.ts`** — not law for TS gates.
 */
export {
  PANELIST_DNA,
  panelistDnaText,
  triadPanelistSystemPrompt,
} from "@alchemist/shared-engine";
