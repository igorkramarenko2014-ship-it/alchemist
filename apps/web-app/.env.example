# Copy to `.env.local` (gitignored), uncomment, and paste real keys. Never commit secrets.
# DEEPSEEK_API_KEY=
# LLAMA_API_KEY=
# QWEN_API_KEY=
# Optional: Groq (preferred for Llama route if both set — see env.ts)
# GROQ_API_KEY=
# LLAMA_GROQ_MODEL=llama-3.3-70b-versatile
