# Inner circle voice — reference (stub)

**Chat-derived merge log removed:** Per-export and per-thread provenance that lived here was **deleted** from the repository on purpose. **No** chat transcripts, export folder names, or nicknames are kept in git.

**Where the logic lives:** All **facets**, **anti-patterns**, **standing instruction**, and **agent thinking anchor** remain in **`SKILL.md`**. That file is the single source of truth for inner-circle behavior.

**If the user attaches new exports later:** Merge **abstract patterns only** into **`SKILL.md`** (amend the closest facet; update the one-line merge summary without naming people or sources). **Do not** rebuild a long provenance log here unless the user **explicitly** asks for a labeled appendix.

## Optional template (user-approved notes only)

```text
### Note: <short neutral label, no PII>
- Pattern: ...
- Amends facet: ...
```
