node_modules
# Local vendored toolchains / IDE (never commit)
.node/
.bin/
.rustup/
.cargo/
.vscode/

.pnpm-store
.pnpm-debug.log*
.turbo
coverage
*.log
# Rust: avoid indexing/committing the crates.io mirror (hundreds of unrelated READMEs)
.cargo/registry
.cargo/git
target/

.next
.expo
dist
.data
*.tsbuildinfo
.DS_Store
.env*.local
*.fxp
# PNH warfare CLI output (pnpm pnh:model-warfare)
pnh-report.json
# Opt-in PNH attack audit (ALCHEMIST_PNH_HISTORY_JSONL=1)
tools/pnh-history.jsonl
# Last PNH simulation run (verify:harsh + pnh:simulate)
tools/pnh-simulation-last.json
# PNH enforcement proposal artifact (pnpm pnh:simulate / pnpm pnh:proposals)
tools/pnh-proposals.jsonl
# PNH war game report (pnpm pnh:wargame)
tools/pnh-wargame-last.json
__pycache__
*.pyc
.venv
build
*.jucer

# Gate calibration local artifact (pnpm test:real-gates)
tools/gate-calibration-output.json
# IOM self-heal proposals (pnpm igor:heal → pnpm igor:apply)
tools/iom-proposals.jsonl
# IOM audit trail (pnpm iom:snapshot)
tools/iom-snapshots.jsonl
# Optional SOE hint migration I/O (pnpm soe:migrate)
tools/soe-hints-legacy.jsonl
tools/structured-soe-hints.jsonl
# Accepted IOM cell proposals (pnpm igor:apply)
tools/iom-accepted-proposals/
# Serum 2 mod discovery output (pnpm serum2:discover-mod-matrix)
tools/serum2-discovery/

# Verify release receipts (local / CI; regenerate via pnpm verify:harsh)
artifacts/verify/
.artifacts/verify/
