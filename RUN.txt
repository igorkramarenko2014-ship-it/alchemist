First time / “won’t run”: open the repo root folder (has **apps/** + **packages/**), then **`pnpm dev`**.
**`pnpm dev`** (and **`go`**, **`web:dev:fresh`**, **`dev:recover`**, **`vst/` → `pnpm dev`**) runs **`scripts/happy-panda.mjs` first** — monorepo root, Node, pnpm, `.env.local` warn. Skip: **`ALCHEMIST_SKIP_PANDA=1`**.
If **node_modules** was never installed, **`apps/web-app/scripts/dev-server.mjs`** auto-runs **`with-pnpm.mjs install`** once when you start dev.

Copy this ENTIRE line into Terminal and press Enter:
────────────────────────────────────────────────────────────────────────────────

(source ~/.zprofile 2>/dev/null; source ~/.zshrc 2>/dev/null; export PATH="/opt/homebrew/bin:/usr/local/bin:$PATH"; cd "/Users/igorkramarenko/Desktop/Vibe Projects" && chmod +x scripts/go.sh && ./scripts/go.sh)

Fresh dev (clears stale Next/Turbo cache first):
────────────────────────────────────────────────────────────────────────────────
(source ~/.zprofile 2>/dev/null; source ~/.zshrc 2>/dev/null; export PATH="/opt/homebrew/bin:/usr/local/bin:$PATH"; cd "/Users/igorkramarenko/Desktop/Vibe Projects" && chmod +x scripts/go.sh && ALCHEMIST_FRESH=1 ./scripts/go.sh)
Or:  pnpm go:fresh

────────────────────────────────────────────────────────────────────────────────
This loads your shell config, finds Node/pnpm, installs deps, and starts **only the web app** (not Expo).
Web app: LOOK AT THE TERMINAL — a cyan box shows http://127.0.0.1:PORT.
         **`pnpm dev`**, **`pnpm go`**, **`pnpm web:dev:fresh`**, **`pnpm dev:recover`**, and **`vst/` → `pnpm dev`** use **`scripts/dev-alchemist-port.mjs`** — they try to **free :3000** (SIGTERM listeners) then **bind 3000** (same as **`pnpm dev:3000`**).
         If you run **`pnpm --filter @alchemist/web-app dev`** without that wrapper, the dev-server **scans 3000–3120** when the preferred port is busy.
         Dev binds 0.0.0.0 by default (loopback-only: HOST=127.0.0.1).
         If the banner shows :3001 (or any port ≠ 3000), **:3000 was still busy** after the auto-free step — do NOT open :3000; you will see another app’s errors (500/404). A yellow warning appears above the cyan box when this happens.

**Terminal says “Ready” but the app “won’t run” in the browser:** you almost certainly opened the **wrong port** (often **:3000** while Alchemist is on **:3003**, etc.). Use **only** the **`http://127.0.0.1:PORT`** line inside the **cyan** box. To see what owns **3000**: `lsof -nP -iTCP:3000 -sTCP:LISTEN` — quit that process if you want Alchemist to bind **3000** next time.

**Want to avoid :3000 entirely:** from repo root run **`pnpm dev:3010`** (or **`pnpm fresh:3010`** to wipe `apps/web-app/.next` on boot). Open **`http://127.0.0.1:3010`** from the banner.

**“Panda very sad” / nothing works:** **`pnpm alc:doctor`** (not `pnpm doctor`) — it prints a short recovery checklist at the end.

**If `pnpm dev` or `./scripts/go.sh` fails:**
  cd "/Users/igorkramarenko/Desktop/Vibe Projects"   # must be the folder with apps/ + packages/
  pnpm install
  pnpm alc:doctor          # Node, Next + workspace packages + env:check (NOT `pnpm doctor` — pnpm built-in)
  pnpm env:check           # .env.local Groq line format only
  pnpm app:repair          # same as web:dev:fresh — full clean (.next + .turbo) then dev (fixes corrupt Next cache / 404 on home)
  pnpm dev:recover         # clears apps/web-app/.next/cache/webpack+swc then dev — lighter than app:repair
  pnpm dev                 # direct web dev-server — use 127.0.0.1 URL from cyan banner, not localhost
  pnpm verify:harsh        # optional sanity: types + Vitest
  pnpm verify:harsh:fire   # same + ALCHEMIST_FIRE_SYNC=1 (refresh FIRE.md metrics after green)
  pnpm harshcheck:fire     # full harshcheck + fire sync after green (local)
  pnpm harshcheck:wasm     # harshcheck + REQUIRE_WASM=1 (real fxp-encoder pkg; run pnpm build:wasm first)
  pnpm check:ready         # env:check + verify:harsh (quick green before dev)
  pnpm verify:keys         # live API check for Groq + DeepSeek + Qwen (.env.local; Qwen uses QWEN_BASE_URL; no keys in output)
  pnpm test:real-gates     # optional: calibration vs live routes (BASE_URL=…; writes gitignored tools/gate-calibration-output.json)
  pnpm build:wasm          # Rust → WASM for browser Export .fxp (after rustup + wasm32-unknown-unknown)
  pnpm vst:observe         # optional: CLI .fxp bridge + IOM vst_observer (see docs/vst-wrapper.md)
  pnpm vst:observe:gate    # optional: offset-map validate hook
  pnpm vst:daemon          # optional: fxp-encoder Rust watcher
  pnpm build:vst           # optional: JUCE VST3 copy when CMake built apps/vst-wrapper
  pnpm fire:sync           # optional: refresh docs/FIRE.md metrics (runs test:engine; needs green tests)
  pnpm iom:status          # optional: offline IOM + engine scale heuristic (same math as pnpm estimate)
  pnpm estimate            # optional: LOC + replacement-cost bands (not professional valuation)
  pnpm web:rebuild         # if next build fails with MODULE_NOT_FOUND in .next — clean + forced web build
  # optional Turbo wrapper: pnpm dev:turbo

**Mobile (Expo) separately:**  pnpm dev:mobile
**Everything at once:**         pnpm dev:all

If you see "Node.js is not installed", run this first (installs Node via Homebrew):
  brew install node
Then run the long command above again.

────────────────────────────────────────────────────────────────────────────────
STILL SEEING AN OLD UI OR OLD TRIAD BEHAVIOUR?
────────────────────────────────────────────────────────────────────────────────

1) Stop the dev server (Ctrl+C).

2) From repo root ("Vibe Projects"), clear Next + Turbo cache and restart dev:
     pnpm web:dev:fresh

   Or only the web app:
     cd apps/web-app && pnpm dev:fresh

3) If you use PRODUCTION mode (next start), you MUST rebuild after code changes:
     pnpm web:rebuild
     pnpm --filter @alchemist/web-app start

4) Hard-refresh the browser: Cmd+Shift+R (Mac) or disable cache in DevTools Network tab.

5) Open the correct folder in Cursor: "Vibe Projects" (repo root), not only "vst/".

   If you only have "vst/" open: from that folder run `pnpm dev` — vst/package.json forwards to the parent repo.
   If **`pnpm: command not found`**: from **`vst/`** run **`npm run dev`** (uses `npx pnpm@9.14.2` via `with-pnpm.mjs`).

**Webpack dev cache / "Restoring pack … failed" / `hasStartTime`:**
  From repo root: `pnpm web:dev:fresh`  OR  `ALCHEMIST_NEXT_SCRUB_WEBPACK=1 pnpm dev` (clears only `apps/web-app/.next/cache/webpack`).

**EMFILE: too many open files, watch** (macOS / WSL / big trees):
  Repo defaults: `apps/web-app/next.config.mjs` webpack **polling** (server + client) in dev;
  `apps/web-app/scripts/dev-server.mjs` sets **WATCHPACK_POLLING=true** when the var is **unset**.
  Opt out of polling: `WATCHPACK_POLLING=0 pnpm dev`
  If Watchpack still complains: `ulimit -n 65536` then restart `pnpm dev` from repo root.

next.config.mjs sets transpilePackages for @alchemist/* so workspace packages are not stuck on an old compiled copy.

If `next build` / `pnpm harshcheck` fails with MODULE_NOT_FOUND inside apps/web-app/.next:
  pnpm run clean && pnpm harshcheck
  # or: pnpm web:rebuild

Browser "Export .fxp" (needs Rust + wasm-pack):
  cd packages/fxp-encoder && pnpm run build:wasm
  # Health: GET /api/health/wasm — see docs/FIRESTARTER.md §10, docs/FIRE.md §C

────────────────────────────────────────────────────────────────────────────────
SAVE TO GITHUB (after every meaningful step — do not lose the project)
────────────────────────────────────────────────────────────────────────────────

From repo root ("Vibe Projects"):

  pnpm save -- "short description of what you changed"

Or:  node scripts/git-save.mjs "your message"

From **vst/** only:  npm run save -- "your message"

First time only — put your real repo URL in ONE command (create empty repo at https://github.com/new first):

  pnpm github:first-push -- "https://github.com/YOUR_USER/YOUR_REPO.git"

(SSH is fine too: git@github.com:YOUR_USER/YOUR_REPO.git)

No URL handy? Run the same command with no args — it prints step-by-step instructions.

After origin exists, `pnpm save -- "…"` commits and pushes for you.

If git complains about loose objects:  rm -f .git/gc.log && git gc --prune=now
